<!DOCTYPE html>
<html>
  <head>
    <title>Unit 2 Assessment</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>To Do List</h1>
    <% if(!todos.length){ %> 
      <h2>Nothing to do.</h2>
      <% } else { %> 
        <ul>
          <% for(let i = 0; i < todos.length; i++){ %>
            <li><%= todos[i].todo %> - Not Done
              <form class='deleteform'action='/<%= i %>?_method=DELETE' method='post'>
              <input type='submit' value='delete'></form></li>
            <% } %> 
        </ul>
        <% } %> 
       <div>
          <form class='addform' action='/' method='post' autocomplete="off">
            <input type='text' name='todo' required>
            <input type='submit' value='Add'>
          </form> 
       </div>
  </body>
</html>
